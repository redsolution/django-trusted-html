"""
Constants
"""

from trustedhtml.classes import List

inherit = List(values=[
    'inherit'
])

auto = List(values=[
    'auto'
])

none = List(values=[
    'none'
])

normal = List(values=[
    'normal'
])

transparent = List(values=[
    'transparent'
])
